import { useSelector } from "react-redux";

const Movie = () => {
  //   const movieList = useSelector((state) => state.Movie.MovieData);
  //   console.log(movieList);
  return <div>this is movie page</div>;
};

export default Movie;
